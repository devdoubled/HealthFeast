import { default as React, default as React } from "react";
import { StyleSheet, View } from "react-native";

const NavbarList = () => {
  return (
    <View>
      <Text>NavbarList</Text>
    </View>
  )
}

export default NavbarList

const styles = StyleSheet.create({})