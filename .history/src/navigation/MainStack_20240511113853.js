import React from "react";
import { StyleSheet, Text, View } from "react-native";
import AskingScreen from "../ "
const MainStack = () => {
  return (
    <View>
      <Text>NGU LOZ</Text>
    </View>
  );
};

export default MainStack;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
});
